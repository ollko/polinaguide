meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: false
run:
  persistenceMount: /data
  command: python3.11 -m data.init_db && python3.11 bot.py
  containerPort: 8080
  servicePort: 80
