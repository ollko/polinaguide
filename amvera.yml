meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  persistenceMount: /data
  containerPort: 80
  servicePort: 80
  command: alembic upgrade head && python3.11 bot.py
